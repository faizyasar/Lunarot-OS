import base64
import os

# Let's decode the GLB and Cover Art directly from the prompt manifest
# We can read the user's input from the active conversation or write the base64 chunks directly
with open(r'c:\Users\faizy\Documents\Lunarot Engine\Lunarot-Tarot-Engine-1.0\scratch\export_assets.py', 'w', encoding='utf-8') as f:
    pass
